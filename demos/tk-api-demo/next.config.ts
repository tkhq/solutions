import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Add your local network IP here to allow access from other devices on the same WiFi.
  // Example: allowedDevOrigins: ['192.168.1.42']
  // allowedDevOrigins: [],
};

export default nextConfig;
